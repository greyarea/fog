%% -*- mode: erlang -*-

{application, fog,
 [{description, "Facebook Open Graph"},
  {vsn, git},
  {id, "fog"},
  {mod, {fog_app, []}},
  {applications, [kernel, stdlib, sasl, lager, ibrowse, crypto, public_key, ssl]}]}.

